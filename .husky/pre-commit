#!/bin/sh

# 1. Run biome check + format on all source files
npx @biomejs/biome check --write src test *.json *.js *.ts 2>/dev/null || npx @biomejs/biome check --write src test

# 2. Re-stage all formatted files
git add .

# 3. Build — ensures the package compiles before committing
npm run build
